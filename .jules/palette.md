## 2025-02-18 - Search Shortcut Pattern
**Learning:** Adding a `/` shortcut with a visual `<kbd>` hint inside the input improves discoverability without cluttering the UI. The hint should be hidden on focus or when text is present to avoid visual collision.
**Action:** Apply this pattern to all global search inputs in the application to create a consistent power-user experience.
